# Runtime/build dependencies not already supplied by the pinned PyTorch base.
# Keep this list minimal: challenge inference does not run evaluation or plots.
jaxtyping==0.3.11
ninja==1.13.0
numpy==2.4.6
nvtx==0.2.13
pillow==12.3.0
rich==15.0.0
tqdm==4.70.0
